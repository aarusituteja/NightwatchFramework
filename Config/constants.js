/**
 * All the content types
 */
const tideContentTypes = {
  'page': 'page',
  'landing page': 'landing_page',
  'news': 'news',
  'publication': 'publication',
  'publication page': 'publication_page',
};

/**
 * Project names
 *
 * Used for identify a project so we handle custom features.
 * This should match `LAGOON_PROJECT` value.
 * Add more when we eed.
 */


module.exports = {
  tideContentTypes,

};
